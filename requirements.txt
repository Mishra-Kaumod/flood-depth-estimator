# Base dependency list for local tooling and generic Python environments.
# Production containers install from Dockerfile / requirements-local.txt.
-r requirements-local.txt
numpy>=1.26.0
gunicorn>=22.0.0
